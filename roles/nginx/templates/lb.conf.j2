events {}
http {
    upstream backend {
        {% for host in groups['web'] %}
        server {{ hostvars[host].ansible_default_ipv4.address }}:808;
        {% endfor %}
    }
    server {
        listen 80;
        location / {
            proxy_pass http://backend;
        }
    }
}